import React from "react";
import FormPage from "../components/FormPage";

type Props = {};
const SetAircraft: React.FC<Props> = () => {
  return <div>set aircraft</div>;
};

export default FormPage(SetAircraft);
